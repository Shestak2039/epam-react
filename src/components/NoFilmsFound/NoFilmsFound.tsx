import React from 'react';

import './no-films-found.scss';

const NoFilmsFound: React.FunctionComponent = () => (
    <h2 className="no-films-found-title">No film found</h2>
);

export default NoFilmsFound;
